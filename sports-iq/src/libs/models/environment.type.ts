export interface Environment {
  env: string;
  //   apiKey: string;
  baseUrl: string;
  issuer: string;
  clientId: string;
  responseType: string;
  scope: string;
  showDebugInformation: boolean;
  redirectUri: string;
}
